# run_scrape.bat の説明（日本語の解説はここに置く）

`run_scrape.bat` は **ASCII だけで書くこと**。日本語コメントを足すと動かなくなる。

## なぜ ASCII 限定なのか（2026-08-10 に判明した実バグ）

cmd.exe は .bat を「実行しながらバイト位置で読み直す」ため、ファイル中にマルチバイト
（UTF-8）の文字があると解析位置がずれ、**コメント行の途中から先をコマンドとして実行し始める**。

実際に起きていたこと:

```
'位に出る）。' is not recognized as an internal or external command,
```

これは `rem` 行「…相場降順の /premium では誤りほど上位に出る）。」の後半である。つまり
`rem` が失われて実行された。この状態では bat は必ず即死するので、

- **`scrape_log.txt` は 2026-07-21 で止まったまま**（bat が1行も書けずに落ちる）
- スケジューラ経由でも `LastTaskResult=1` で終わる

`chcp 65001` を先頭に置いても、UTF-8 BOM を付けても直らない（両方試して再現）。
唯一の確実な対処は **bat 本体から日本語を無くすこと**。解説はこのファイルに書く。

## 定例更新が動いていなかった件（2026-08-10 修正）

`scrape_schedule_setup.bat` は一度も実行されておらず、**タスクスケジューラに登録が無かった**。
プロジェクトメモの「毎日8:00/20:00に自動更新」は事実ではなく、更新は手動実行の時だけ
起きていた（最終更新が丸1日以上前になっていた原因）。

現在は以下2つのタスクを登録済み:

| タスク名 | 実行時刻 |
| --- | --- |
| `HatsukoreScrape_AM` | 毎日 08:00 |
| `HatsukoreScrape_PM` | 毎日 20:00 |

確認: `schtasks /query /tn HatsukoreScrape_AM /v /fo LIST`
削除: `schtasks /delete /tn HatsukoreScrape_AM /f`（PM も同様）

※ ログオン中のユーザーとして走るので、PC が起動していない時間帯の分は流れない。

## 各ステップの意図

1. **データ更新** — `scrape` → 画像バックフィル → コラボ再エンリッチ → 相場取得 →
   相場の洗い直し → 一番くじ各賞の相場。
   `prices:recheck` は既存の相場も洗い直す。`scrape:prices` は未取得を優先するので、
   一度付いた誤りはこれを回さないと永久に残る（しかも相場降順の `/premium` では
   誤りほど上位に出る）。
2. **チェック** — `audit:selftest`（監査自身が鳴るべき入力で鳴るか。検査は本番を壊さないので
   壊れていても咎められず「0件」と報告してしまう）→ `audit`（表示品質の常設監査。ERROR が
   あれば「きれい」と言えない）→ `audit:facts`（一次情報との突合）。

`npm run audit:page`（描画監査）はここに入っていない。dev サーバか本番URLが要るため、
別途手で回す。**これを回すまで「全部チェックした」と言わないこと。**
